/** @type {import('tailwindcss').Config} */
export default {
    // Note: In Tailwind v4, darkMode config is removed - use @custom-variant in CSS instead
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}

